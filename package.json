{
  "name": "vidgrab",
  "version": "1.0.0",
  "description": "Download videos from YouTube (and other platforms) from a VPS without getting blocked. Uses Cloudflare WARP for residential IPs and a PO token provider for bot detection bypass.",
  "main": "dist/cli.js",
  "bin": {
    "vidgrab": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.7.0"
  }
}
